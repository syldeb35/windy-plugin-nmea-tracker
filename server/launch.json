{
  "configurations": [
    {
      "type": "debugpy",
      "request": "launch",
      "name": "Debug NMEA Server",
      "program": "${workspaceFolder}/server/nmea_server.py",
      "cwd": "${workspaceFolder}/server",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/server"
      },
      "console": "integratedTerminal"
    }
  ]
}